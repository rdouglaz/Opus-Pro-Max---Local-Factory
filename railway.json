{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "streamlit run app.py --server.port=${PORT:-8501} --server.address=0.0.0.0",
    "restartPolicyType": "ON_FAILURE"
  },
  "env": {
    "DEEPGRAM_API_KEY": {
      "description": "Your Deepgram API Key for transcription",
      "required": true
    },
    "OLLAMA_URL": {
      "description": "Ollama URL (leave empty to use default localhost, or set to Railway internal Ollama service)",
      "required": false
    }
  }
}